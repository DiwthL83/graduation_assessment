
<%= form_for(@comment) do |c| %>
  <% if @comment.errors.any? %>
    <div id="">
      <h2>
        <%= pluralize(@comment.errors.count, "error") %> prohibited this article from being saved:
      </h2>
      <ul>
        <% @comment.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
		<%= c.label "Name" %><br>
		<%= c.text_field :commenter_p %>
	</p>

	<p>
		<%= c.label "Title" %><br>
		<%= c.text_area :body_p %>
	</p>

	<p>
		<%= c.submit "Save Message" %>
	</p>

<% end %>
<% end %>
<% form_tag(action: '', method:'post') %>
  <% text_field "Name", :commenter_p %>
  <% text_field "Subject Heading", :title_p %>
  <% text_field "Message Text", :body_p %>
  <% submit_tag "Save Message" %>
<% end %>